export const locales = ['en', 'es'];
export const defaultLocale = 'en';

// Define how we want to handle locales in URLs
export const localePrefix = 'always'; // Valid values: 'as-needed' | 'always' | 'never'

// This is the list of languages displayed in the language switcher
export const languages = {
  en: 'English',
  es: 'Español'
};
